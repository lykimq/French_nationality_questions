{
    "name": "naturalisation-france-functions",
    "private": true,
    "main": "lib/index.js",
    "engines": {
        "node": "20"
    },
    "scripts": {
        "build": "tsc",
        "build:watch": "tsc --watch",
        "serve": "npm run build && firebase emulators:start --only functions",
        "deploy": "firebase deploy --only functions:synthesizeFrenchSpeech",
        "logs": "firebase functions:log"
    },
    "dependencies": {
        "@google-cloud/text-to-speech": "^5.8.0",
        "firebase-admin": "^12.7.0",
        "firebase-functions": "^6.3.0"
    },
    "devDependencies": {
        "@types/node": "^22.10.0",
        "typescript": "^5.9.2"
    }
}
