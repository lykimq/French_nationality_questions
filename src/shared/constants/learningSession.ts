/**
 * Size of the prioritized "recommended" flashcard session (home + FlashCard flow).
 * Kept in sync with mastery daily goal defaults in storage.
 */
export const RECOMMENDED_SESSION_QUESTION_COUNT = 20;
